# ⛔ 이 폴더는 아카이브입니다 — 여기서 작업하지 마세요

**2026-08-03 cutover 완료**로 리워크가 운영 사이트가 되었습니다.
이 폴더(`Test_version` 레포)는 그 시점에서 **동결**되었습니다.

## 작업 위치
```
/mnt/c/Users/kyefy/OneDrive/바탕 화면/개인자료/03_WCHP/Whale-Corp-main
원격: github.com/HotCake0/Whale-Corp   브랜치: main   (push = 즉시 운영 배포)
```

여기서 고치면 **배포되지 않고**, 두 레포가 갈라져 이 프로젝트의 단골 사고
("같은 화면이 코드로는 여러 벌")가 레포 단위로 재발합니다.

## 이 폴더에만 남아 있는 것 (참고용 동결분 — 운영 레포엔 없음)
| 자산 | 정체 |
|---|---|
| `와이어프레임/` | manyfast IA 와이어프레임 25페이지 (리워크의 출발점) |
| `고래상사 디자인 시스템 (standalone).html` | 22MB 디자인 툴 번들 산출물 (읽는 건 `.md` 쪽) |
| `index.before-assets.html` | site.css/js 분리 직전 홈 스냅샷 |
| `.impeccable/` | critique 스냅샷 이력 |

그 외 배포 자산·`src/`(빌드 소스)·`ROADMAP.md`는 **전부 운영 레포에 바이트 동일로 이관**되었고,
운영 레포 단독으로 `python3 src/build.py` 재현(diff 0)·`node src/test_pages.js`(69/69) 검증을 마쳤습니다.

상세 경위 = 운영 레포 `ROADMAP.md` §0-12(cutover 실행 기록)·부록 E-1.
